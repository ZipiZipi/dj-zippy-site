---
// Prerendered to dist/404.html — Cloudflare Pages serves this for any unknown
// path that never reaches the worker. Unknown paths *under* /sr are handled by
// src/pages/sr/[...slug].astro, which renders the Serbian copy with a real 404.
import NotFoundPage from '../components/pages/NotFoundPage.astro';
---

<NotFoundPage lang="en" />
